{
  "type": "module",
  "dependencies": {
    "@fastify/cookie": "^9.3.1",
    "@fastify/cors": "^9.0.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/jwt": "^8.0.0",
    "@fastify/sensible": "^5.5.0",
    "fastify": "^4.26.1",
    "fastify-bcrypt": "^1.0.1",
    "fastify-sqlite": "^1.1.0"
  },
  "scripts": {
    "test": "NODE_ENV=test node -r dotenv/config --test --watch ./tests/**/*.test.js",
    "test1": "NODE_ENV=test nodemon -r dotenv/config ./tests/v1/app.test.js",
    "dev": "NODE_ENV=development nodemon -r dotenv/config server.js",
    "start": "NODE_ENV=production node server.js"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "nodemon": "^3.1.0"
  }
}
